# Research Topic Generator

A rule-based research topic generator for students.
Built with HTML, CSS, and Vanilla JavaScript.

## 🚀 How to Deploy to Vercel

You have two main options to deploy this project:

### Option 1: Using Vercel CLI (Recommended for quick tests)

1.  **Install Vercel CLI** (if you haven't already):
    ```bash
    npm install -g vercel
    ```
2.  **Deploy**:
    run the following command in your terminal:
    ```bash
    vercel
    ```
    Follow the prompts (Log in -> Yes to settings -> confirm directory).

### Option 2: Using GitHub

1.  Create a new repository on GitHub.
2.  Push your code:
    ```bash
    git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
    git branch -M main
    git push -u origin main
    ```
3.  Go to [Vercel Dashboard](https://vercel.com/new).
4.  Import your GitHub repository.
5.  Vercel will automatically detect the static site and deploy it.

## 📁 Project Structure

-   `index.html`: Main entry point.
-   `style.css`: Styles.
-   `script.js`: Logic for generating topics.
-   `vercel.json`: Configuration for Vercel deployment.
